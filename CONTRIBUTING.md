# 参与贡献

感谢参与 WeLearn Studio。提交代码前，请先阅读[架构说明](docs/ARCHITECTURE.md)和[独立实现规范](docs/CLEAN_ROOM.md)。项目不接受从其他工程复制、反编译或机械改写得到的代码。

## 开发环境

统一使用以下环境：

- Windows 10 或 Windows 11（64 位）；
- Python 3.12 或更高版本；
- PowerShell 7（`pwsh`）；
- Git。

在项目目录中执行：

```powershell
py -3.12 -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\python.exe -m pip install -e ".[dev]"
```

## 开发原则

- 先在领域层表达业务规则，再接入应用控制器和 Qt 界面。
- 平台 JSON、表单字段和协议差异应限制在远程适配器中。
- 网络结果必须明确区分已接受、已拒绝和未知，不使用误导性的“平台已成功”表述。
- 保持账号、课程、配置、日志和任务状态相互隔离。
- 所有界面修改都要兼顾深色主题、高 DPI、文字裁切、对齐和键盘操作。
- 只添加能够解决实际复杂度的抽象，不进行与当前需求无关的重构。

## 提交流程

1. 从最新的 `main` 创建工作分支。
2. 为行为变化添加或更新有针对性的自动化测试。
3. 完成实现，并同步修改受影响的规格或使用说明。
4. 运行完整验证：

```powershell
pwsh -NoProfile -File .\scripts\verify.ps1
```

5. 检查差异中不包含账号文件、日志、个人截图、构建产物或本地配置。
6. 提交说明应描述最终行为，而不是只罗列修改过的文件。

## 测试边界

- 自动化测试不得登录或访问真实平台。
- 测试账号、课程和协议响应必须是明显虚构的合成数据。
- 协议观察和对应实现应分别记录，不能把真实响应原文直接提交到仓库。
- 新增远程响应分支时，需要覆盖正常、拒绝、字段缺失和无法识别等情况。
- 修复 UI 问题时，需要补充对应控件状态或布局测试；必要时在隔离配置下进行人工检查。

## 敏感信息

禁止提交：

- 账号、密码、Cookie、授权令牌和会话标识；
- 真实课程记录、学校信息和学习记录；
- 包含个人信息的截图、日志或抓包文件；
- `.venv`、`build`、`dist`、缓存和本地工作区设置。

发现敏感信息时，应立即停止提交并按[安全说明](SECURITY.md)处理。

## 合并请求

合并请求应包含修改目的、用户可见变化、验证结果和已知限制。涉及远程协议时，还应说明变化属于协议观察、兼容处理还是错误修复。
