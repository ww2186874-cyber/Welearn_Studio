# 安全说明

WeLearn Studio 会在当前进程内使用账号凭据建立用户自己的登录会话。账号文件、会话信息和平台数据都应按敏感信息处理。

## 支持范围

安全修复以仓库当前的 `main` 分支和最新发布版本为准。过期源码快照、第三方修改版和非官方构建不在支持范围内。

## 报告安全问题

请不要在公开 Issue、讨论区或 Pull Request 中披露漏洞细节、账号信息或真实平台响应。

优先使用 GitHub 仓库 `Security` 页面中的私密漏洞报告功能。若该入口尚未启用，请先通过仓库所有者 GitHub 主页提供的私密联系方式沟通，再发送经过脱敏的说明。

报告建议包含：

- 受影响的版本或提交；
- 可以复现问题的最小步骤；
- 可能造成的影响；
- 已删除账号、Cookie、令牌和个人数据的日志或示例。

## 凭据泄漏处理

如果怀疑账号、Cookie、令牌或会话标识已经泄漏：

1. 立即停止使用相关文件和程序实例。
2. 在平台端退出相关会话，并修改受影响账号的密码。
3. 删除公开位置中的账号文件、日志、截图或构建产物。
4. 通过私密渠道通知仓库所有者，并只提供脱敏信息。

仅从 Git 历史中删除文件不能保证凭据失效，撤销会话和修改密码仍然是必要步骤。

## 数据处理原则

- 密码只保留在当前程序进程内，不写入工作区配置。
- 工作区配置不得保存 Cookie、授权令牌或完整请求头。
- 日志必须隐藏认证字段，不输出密码、Cookie、令牌或会话标识。
- 账号导入文件由用户自行保管，不应存放在仓库目录或云端公开目录。
- 公开截图前必须检查账号、昵称、学校、课程、学习记录和本机路径。

## 开发与测试要求

- 自动化测试不得访问真实平台。
- 测试夹具必须使用明显虚构的账号、课程标识和协议响应。
- 不得把真实抓包、服务器响应或认证材料直接提交到仓库。
- 新增诊断日志时，应先验证异常路径不会泄漏请求头、表单凭据或响应中的个人数据。
- 发布前应扫描仓库、构建目录和压缩包中的账号文件、日志、个人路径和截图。

## 使用者责任

只在自己有权访问的账号和课程范围内使用本软件。用户应自行保护账号文件，并了解远程请求被接口接受不等于平台最终统计已经变化。
