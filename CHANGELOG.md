# 变更记录

## 0.1.2 - 2026-08-11

- 重新协调账号、课程工作区与运行概览的背景层级、留白、字号、边界和操作按钮，减少不必要的色彩与嵌套面板。
- 统一账号选中状态、单元列表、任务参数与底部操作栏，并保持右侧运行概览在切换账号时的布局稳定。
- 使用高对比度自定义复选框统一单元与小课选择，整个文字和行区域均可点击，并修正高 DPI 下的对齐和裁切问题。
- 整理配置中心和小课选择窗口，统一列表、空状态、工具栏、底部操作区和键盘焦点反馈。
- 改善运行指标和结构化日志的对齐、间距与可读性，不改变已验证的计时、并发和远程请求逻辑。
- 增加复选框整行切换、配置操作和对话框按钮的真实鼠标点击回归测试。

## 0.1.1 - 2026-08-10

- 根据真实平台复测恢复兼容计时协议：先读取小课状态，必要时初始化，随后立即发送初始心跳并在每分钟重放一次基线状态。
- 恢复旧版 SCO 公共请求字段，移除未被稳定采纳的额外计时字段和最终 `updatecmitime` 请求；18 个三分钟任务的真实平台统计已验证接近计划的 54 分钟。
- 将 `.har` 加入 Git 忽略，并新增不含账号、课程标识、Cookie 或令牌的协议回归夹具。
- 保持整分钟平均分配和余数舍弃规则，将任务准备、远程操作与批次执行从 Qt 控制器中拆分为独立应用服务。
- 拆分协作式批次执行器的单任务、批次和结果收集职责，并增加 `33` 至 `100` 并发范围的回归测试。

## 0.1.0 - 2026-08-09

- 建立独立的 WeLearn Studio 产品规格、架构、协议边界、开发流程和发布流程。
- 新增适配高 DPI 的 PySide6 三栏工作台，集中管理账号、课程、单元、小课选择、完整课程预设、任务设置和运行状态。
- 新增经过身份验证的课程读取，并使用已接受、已拒绝和未知结果明确描述远程响应；诊断信息不包含敏感字段。
- 新增整分钟时长规划、平台有效时长计算、固定并发批次、协作式取消和任务启动前的最低时长验证。
- 新增账号秒级倒计时、真实任务进度、活动批次信息、已运行与剩余时间，以及可逐条阅读的小课运行日志。
- 新增不保存密码的原子化本地设置、账号与课程数据隔离、离线协议夹具、界面测试、Ruff 检查和 Windows 持续集成。
